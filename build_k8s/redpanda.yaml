apiVersion: v1
kind: Service
metadata:
  name: redpanda
  namespace: default
  labels:
    app: redpanda
spec:
  ports:
  - name: external
    port: 9092
    targetPort: 9092
  - name: admin
    port: 9644
    targetPort: 9644
  selector:
    app: redpanda
  type: ClusterIP
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redpanda
  namespace: default
spec:
  serviceName: redpanda
  replicas: 1
  selector:
    matchLabels:
      app: redpanda
  template:
    metadata:
      labels:
        app: redpanda
    spec:
      # initContainer runs as root to chown the EBS volume to redpanda user (101:101)
      # before the main container starts. This avoids the Permission Denied error
      # that occurs because EBS volumes mount as root:root by default.
      initContainers:
      - name: fix-data-permissions
        image: busybox:1.36
        command: ['sh', '-c', 'chown -R 101:101 /var/lib/redpanda/data && chmod 755 /var/lib/redpanda/data']
        volumeMounts:
        - name: datadir
          mountPath: /var/lib/redpanda/data
        securityContext:
          runAsUser: 0  # Must run as root to chown
      containers:
      - name: redpanda
        image: docker.redpanda.com/redpandadata/redpanda:v23.2.19
        command:
        - rpk
        - redpanda
        - start
        - --mode=dev-container
        - --smp=1
        - --memory=1Gi
        - --kafka-addr=PLAINTEXT://0.0.0.0:9092,OUTSIDE://0.0.0.0:9092
        - --advertise-kafka-addr=PLAINTEXT://redpanda:9092,OUTSIDE://redpanda:9092
        ports:
        - name: external
          containerPort: 9092
        - name: admin
          containerPort: 9644
        resources:
          limits:
            cpu: "1"
            memory: 2Gi
          requests:
            cpu: "250m"
            memory: 1Gi
        volumeMounts:
        - name: datadir
          mountPath: /var/lib/redpanda/data
  volumeClaimTemplates:
  - metadata:
      name: datadir
    spec:
      accessModes: [ "ReadWriteOnce" ]
      storageClassName: gp2
      resources:
        requests:
          storage: 5Gi
